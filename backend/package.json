{
  "name": "@arcvoucher/backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "format": "prettier --check .",
    "test": "tsx --test"
  },
  "dependencies": {
    "dotenv": "latest",
    "viem": "latest"
  },
  "devDependencies": {
    "@types/node": "latest",
    "prettier": "latest",
    "tsx": "latest",
    "typescript": "latest"
  }
}
